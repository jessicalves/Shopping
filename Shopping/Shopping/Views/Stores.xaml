<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:Shopping.ViewModels"
    x:Class="Shopping.Views.Stores">

    <ContentPage.BindingContext>
        <viewModels:StoresViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <FlexLayout JustifyContent="SpaceBetween">
                <Entry Text="{Binding SearchWord}" Placeholder="Pesquisa" BackgroundColor="LightGray" WidthRequest="310" HorizontalTextAlignment="Center" Margin="5,0,0,0"/>
                <Button Command="{Binding SearchCommand}" Text="OK"/>
            </FlexLayout>
            <CollectionView ItemsSource="{Binding Establishments}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding Name}" TextColor="{StaticResource TextPrimaryColor}"/>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>

